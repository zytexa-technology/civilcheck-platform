// Kept so the original `import { colors, radius } from "../theme/colors"` path
// still resolves. New code should import from '../theme' instead.
export { colors, radius, spacing, typography, SCREEN_PADDING } from './index'
export type { Colors } from './index'
